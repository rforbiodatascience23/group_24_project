---
title: "Clean file"
format: html
editor: visual
---

Now we need to clean our data First we will check the sructure of the data

```{r}
str(human_raw_data)

```

```{r}
head(human_raw_data)
```

## Next we will convert the dataset into tibble -\> We have to check why. What do you thing?

```{r}

#human_data<- human_raw_data |> 
 # as_tibble()
#head(human_data)
```

First we need to fix the columns names

```{r}

```

We observe that there are many variable in column names, so we need to separate them

```{r}
human_raw_data|> 
  pivot_longer(
    cols = !(gene_id:symbol),
    names_to = c("cell_line", "treatment"), 
    names_sep = ".",
    values_to = "expression levels"
  )
```
